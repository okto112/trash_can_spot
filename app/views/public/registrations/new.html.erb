<div class="col-4 m-auto">
  <div class="mt-4 pt-4 pb-5 border rounded bg-white">
    <h3 class="text-center m-4 border-bottom border-success">新規ユーザー登録</h3>

    <%= form_with model: @user, url: registration_path(resource_name), local: true do |f| %>
      <%= render "public/shared/error_messages", resource: resource %>
      <div class="m-auto w-50">
        <div class="field">
          <%= f.label :name, "ユーザー名" %><br>
          <%= f.text_field :name, autofocus: true, class:"w-100" %>
        </div>

        <div class="field">
          <%= f.label :email, "メールアドレス" %><br>
          <%= f.email_field :email, autocomplete: "email", class:"w-100" %>
        </div>

        <div class="field">
          <%= f.label :password, "パスワード" %>
          <em>(6文字以上)</em>
          <%= f.password_field :password, autocomplete: "new-password", class:"w-100" %>
        </div>

        <div class="field">
          <%= f.label :password_confirmation, "パスワード" %><br>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"w-100" %>
        </div>

        <div class="actions text-center">
          <%= button_tag type: "submit", class: "btn btn-success mt-4 w-50" do %>
           <i class="fa-solid fa-user-plus"> 新規登録</i>
          <% end %>
        </div>
      </div>
    <% end %>

    <div class="mt-4 text-center">
      <h4>既に登録済みの方</h4>
      <p>
        <%= link_to 'こちら', new_user_session_path %>
        からログインしてください。
      </p>
    </div>
  </div>
</div>